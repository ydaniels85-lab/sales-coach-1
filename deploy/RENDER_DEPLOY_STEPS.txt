FIN-TASTIC RENDER DEPLOYMENT — SALES COACH + CLIENT CAPTURE
==========================================================

1. Extract the ZIP.
2. Replace the complete contents of the current private Git repository.
3. Commit and push all changes.
4. Open the existing Render web service.
5. Confirm the environment variable:
   DEFAULT_CREDIT_REPORT_PDF_PASSWORD=DN13084
6. Choose:
   Manual Deploy -> Clear build cache & deploy
7. Open:
   https://YOUR-RENDER-URL/api/health
8. Confirm:
   databaseReady: true
   authenticationRequired: false
   accessMode: open
9. Open the main service URL.

THE UPDATE ADDS
---------------
- Manual New Client creation.
- Single and Joint application capture.
- Primary and spouse personal details.
- Employment, income and household budget.
- Primary and spouse banking details.
- Expanded Sales Coach with call opening, qualifying questions and objections.
- Credit Profile Investigation rule below R1,000 monthly instalments.
- R3,000 fee: once-off, 2, 3 or 4 months.

No database schema migration is required. New information is stored in the existing JSON client payload.

WARNING
-------
Authentication remains disabled. Anyone with the service URL can access stored client records.
